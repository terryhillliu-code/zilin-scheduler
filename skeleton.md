# 知微 2026-03-03
> 54F 13662L

## Documents/Library/fix_klib_core.py (111L)
- patch_file(file_path)
## Documents/Library/klib_db.py (43L)
- get_db_connection(timeout)
## Documents/Library/klib_hybrid.py (489L)
- get_db()
- search_fts(query, top_k)
- search_vector(query, top_k)
- rrf_fusion(fts_results, vector_results, top_k)
- get_book_details(book_ids)
- get_chroma_metadata(doc_ids)
- extract_from_vector_results(vector_results, fused)
- hybrid_search(query, top_k)
- cmd_search(query, top_k)
- cmd_stats()
- main()
## Documents/Library/klib_organize.py (238L)
- get_file_hash(filepath)
- generate_book_id(filepath)
- extract_title_author(filename)
- classify_file(filepath)
- check_duplicate(cursor, file_hash, title)
- organize_directory(source_path, dry_run)
- main()
## Documents/Library/klib_query.py (115L)
- cmd_stats()
- cmd_list()
- cmd_search(keyword)
- cmd_find(title)
- main()
## Documents/Library/klib_scan.py (243L)
- get_file_hash(filepath, chunk_size)
- generate_book_id(filepath)
- normalize_title(title)
- extract_title_author(filename)
- infer_category_priority(filepath)
- detect_language(filepath)
- find_similar_title(cursor, title)
- scan_directory(root_path, db)
- main()
## Documents/Library/klib_vectorize.py (340L)
- get_db()
- run_docker(cmd, timeout)
- check_container()
- get_chromadb_count()
- extract_content(book_id, file_path, file_type)
- vectorize_file(book_id, file_path, title)
- cmd_status()
- cmd_pending(priority)
- cmd_vectorize(priority, book_id)
- main()
## zhiwei-bot/agent_chain.py (237L)
- class AgentChain 
  - detect_chain(message)
  - execute(chain_name, message, session_id)
  - format_result(chain_result)
- detect_chain_intent(message)
- execute_chain(chain_name, message, session_id)
- detect_chain(message)
- execute(chain_name, message, session_id)
- format_result(chain_result)
## zhiwei-bot/article_writer.py (261L)
- retrieve_from_knowledge_base(topic, top_k)
- retrieve_from_web(topic)
- generate_article(topic, context)
- format_article(topic, content, context)
- write_article(topic, user_id)
- main()
## zhiwei-bot/command_handler.py (761L)
- init_command_handler(global_reply_message, global_reply_card, global_call_openclaw_agent, global_query_knowledge_base, global_get_memory, global_add_to_history, global_get_history, global_is_article_url, global_is_video_url, global_summarize_url, global_handle_video_async, global_extract_video_url, global_TaskLogger, global_detect_chain_intent, global_execute_chain, global_IntentRouter, global_save_active_user, global_load_active_user, global_chat_history, global_pending_voice, global_pending_image, global_pending_review, global_MAX_HISTORY, global_RATE_LIMIT_SECONDS, global_user_last_request, global_memory_cache, global_article_writer, global_tech_compare)
- show_help()
- handle_text_async(text, user_id, message_id)
- get_session_id(user_id)
- get_quick_status()
- check_rate_limit(user_id)
## zhiwei-bot/feishu_api.py (138L)
- init_feishu_api(global_client)
- reply_message(message_id, text)
- send_direct_message(user_id, text)
- reply_card(message_id, title, content_text)
## zhiwei-bot/feishu_quota.py (100L)
- record_call(api_type)
- get_stats()
## zhiwei-bot/intent_router.py (70L)
- class IntentRouter 
  - route(cls, message)
  - explain(cls, message)
- route(cls, message)
- explain(cls, message)
## zhiwei-bot/media_handler.py (372L)
- init_media_handler(global_client, global_reply_message, global_task_logger, global_pending_image, global_time)
- download_image(message_id, image_key)
- compress_image_base64(image_path, max_size)
- analyze_image_base64(image_base64, question)
- handle_image_async(message_id, image_key, user_id)
- extract_video_url(text)
- extract_article_url(text)
- is_article_url(text)
- is_video_url(text)
- summarize_url(url)
- handle_video_async(text, message_id, user_id)
- process_video(text, message_id)
- download_audio(message_id, file_key)
- transcribe_audio(audio_path)
## zhiwei-bot/memory_manager.py (195L)
- class MemoryManager 
  - add_turn(user_msg, assistant_msg)
  - build_context_prompt()
  - reset()
  - save_persistent(key, value)
  - get_persistent(key)
  - get_stats()
- extract_important_info(user_msg, assistant_msg)
- add_turn(user_msg, assistant_msg)
- build_context_prompt()
- reset()
- save_persistent(key, value)
- get_persistent(key)
- get_stats()
## zhiwei-bot/openclaw_api.py (47L)
- class OpenClawClient 
  - call_agent(agent_name, message, session_id, timeout)
- call_agent(agent_name, message, session_id, timeout)
## zhiwei-bot/pdf_parser.py (570L)
- init_pdf_parser(global_client, global_reply_message, global_task_logger, global_time)
- download_pdf(message_id, file_key)
- extract_pdf_text(pdf_path)
- extract_pdf_text_docker(pdf_path)
- extract_pdf_summary(pdf_path, max_pages)
- handle_pdf_async(message_id, file_key, user_id)
- parse_pdf_from_path(file_path)
- extract_pdf_tables(pdf_path, pages)
- tables_to_markdown(table_data)
- extract_tables_summary(pdf_path)
- extract_text_from_pdf_base64(pdf_base64)
- extract_tables_from_pdf_base64(pdf_base64)
- analyze_pdf_content(text, pages, question)
- process_pdf_message(message_id, file_key, user_id)
## zhiwei-bot/task_logger.py (48L)
- class TaskLogger 
  - log_task(cls, task_name, result, details)
  - get_recent(cls, n)
  - search(cls, keyword)
- log_task(cls, task_name, result, details)
- get_recent(cls, n)
- search(cls, keyword)
## zhiwei-bot/tech_compare.py (301L)
- parse_comparison(text)
- retrieve_info(item)
- generate_comparison(item_a, item_b, context)
- format_comparison(item_a, item_b, content, context)
- compare_tech(item_a, item_b, user_id)
- main()
## zhiwei-bot/ws_client.py (563L)
- save_active_user(user_id)
- load_active_user()
- query_knowledge_base(query)
- get_memory(user_id)
- cleanup_pending_images()
- add_to_history(user_id, role, text)
- get_history(user_id)
- call_openclaw_agent(message, session_id, agent)
- do_p2_im_message_receive_v1(data)
- main()
- connection_monitor()
- poll_review_notifications()
- poll_task_notifications()
## zhiwei-scheduler/agent_coordinator.py (189L)
- class AgentCoordinator 
  - analyze_task(task, context)
  - delegate_to_agent(from_agent, to_agent, task_id, action, payload, context, timeout)
  - execute_plan(plan, task_id, context)
- demo_collaboration()
- analyze_task(task, context)
- delegate_to_agent(from_agent, to_agent, task_id, action, payload, context, timeout)
- execute_plan(plan, task_id, context)
## zhiwei-scheduler/claude_runner.py (153L)
- class ClaudeRunner 
  - run(prompt, timeout, skip_permissions)
- run_architect(prompt, **kwargs)
- run_executor(prompt, work_dir, **kwargs)
- run(prompt, timeout, skip_permissions)
## zhiwei-scheduler/dev_coordinator.py (382L)
- class DevCoordinator 
  - process_task(task, source)
- main()
- process_task(task, source)
## zhiwei-scheduler/dev_state.py (213L)
- class DevLock 
  - acquire(timeout)
  - release()
- class DevState 
  - start_task(task_id, description)
  - end_task(task_id, success, modified_files)
  - get_context_for_prompt()
- git_auto_commit(message, work_dir)
- git_revert_last(work_dir)
- acquire(timeout)
- release()
- start_task(task_id, description)
- end_task(task_id, success, modified_files)
- get_context_for_prompt()
## zhiwei-scheduler/dispatch_v2.py (240L)
- get_intent_prompt(user_request)
- call_llm(prompt)
- parse_intent(llm_response)
- load_task_builder()
- build_task_text(intent)
- needs_clarification(intent)
- save_task(user_request, intent, task_text)
- dispatch(user_request)
## zhiwei-scheduler/event_bus.py (235L)
- class AgentMessage 
  - to_dict()
  - to_json()
  - from_dict(cls, data)
  - from_json(cls, json_str)
- class EventBus 
  - subscribe(agent_id, handler)
  - unsubscribe(agent_id)
  - publish(message)
  - request(from_agent, to_agent, task_id, payload, context, timeout)
  - respond(original_msg, payload)
  - poll_messages(agent_id)
  - start_polling(agent_id, handler)
  - stop()
- get_event_bus()
- to_dict()
- to_json()
- from_dict(cls, data)
- from_json(cls, json_str)
- subscribe(agent_id, handler)
- unsubscribe(agent_id)
- publish(message)
- request(from_agent, to_agent, task_id, payload, context, timeout)
- respond(original_msg, payload)
- poll_messages(agent_id)
- start_polling(agent_id, handler)
- stop()
## zhiwei-scheduler/knowledge_pipeline.py (207L)
- load_rules()
- load_state()
- save_state(state)
- get_file_size_mb(filepath)
- match_rule(filepath, rule)
- classify_file(filepath, rules_config)
- process_unsorted()
- scan_and_classify()
## zhiwei-scheduler/lock_manager.py (214L)
- acquire_lock(lock_name, timeout)
- is_stale_lock(lock_file, max_age)
- force_unlock(lock_file)
- get_lock_info(lock_name)
- cleanup_all_locks()
## zhiwei-scheduler/manual_run.py (23L)
## zhiwei-scheduler/model_fallback.py (226L)
- call_with_fallback(primary_model, message, timeout, skip_permissions, work_dir)
- log_fallback(primary, fallback, reason)
- test_fallback()
## zhiwei-scheduler/news_dedup.py (124L)
- load_sent_today()
- get_sent_titles()
- record_sent(titles)
- extract_titles_from_content(content)
- count_new_items(content, min_new)
- should_push(content)
## zhiwei-scheduler/obsidian_linker.py (368L)
- find_related_notes(content, top_k)
- confirm_relations(new_note, candidates)
- generate_wikilinks(relations)
- update_note_with_links(note_path, links)
- create_bidirectional_links(new_note_path, relations)
- link_new_note(note_path, note_content)
- main()
## zhiwei-scheduler/obsidian_vectorize.py (275L)
- get_chromadb_client()
- scan_vault(vault_path)
- compute_hash(content)
- vectorize_note(note_path)
- get_existing_docs_in_chroma()
- sync_to_chromadb(vault_path)
- main()
## zhiwei-scheduler/obsidian_watcher.py (298L)
- class ObsidianEventHandler (FileSystemEventHandler)
  - debounce(key, func, delay)
  - on_created(event)
  - on_modified(event)
  - on_deleted(event)
- start_watching(vault_path)
- main()
- debounce(key, func, delay)
- on_created(event)
- on_modified(event)
- on_deleted(event)
## zhiwei-scheduler/openclaw_api.py (47L)
- class OpenClawClient 
  - call_agent(agent_name, message, session_id, timeout)
- call_agent(agent_name, message, session_id, timeout)
## zhiwei-scheduler/price_cache.py (227L)
- ensure_cache_dir()
- compute_data_hash(data)
- load_price_cache()
- save_price_cache(cache)
- has_price_changed(part_number, new_data, force_refresh)
- update_price_cache(part_number, data)
- get_cached_price(part_number)
- clear_cache(part_number)
- get_cache_status()
## zhiwei-scheduler/pusher.py (335L)
- class DingTalkPusher 
  - send_markdown(title, content)
  - send_text(content)
- class FeishuPusher 
  - send_markdown(title, content)
- class EmailPusher 
  - send(title, content)
- class PushManager 
  - is_quiet_hour()
  - push(title, content, channels, force)
  - flush_pending()
  - send_alert(title, content)
- send_markdown(title, content)
- send_text(content)
- send_markdown(title, content)
- send(title, content)
- is_quiet_hour()
- push(title, content, channels, force)
- flush_pending()
- send_alert(title, content)
## zhiwei-scheduler/retry_decorator.py (170L)
- class RetryScheduler 
  - schedule_retry(original_job_id, run_time, max_retries)
  - clear_retry(job_id)
  - get_retry_count(job_id)
- retry_on_failure(max_attempts, delay_seconds, exponential_backoff, on_retry)
- decorator(func)
- schedule_retry(original_job_id, run_time, max_retries)
- clear_retry(job_id)
- get_retry_count(job_id)
- wrapper(*args, **kwargs)
## zhiwei-scheduler/retry_failed.py (41L)
- main()
## zhiwei-scheduler/risk_checker.py (85L)
- class RiskChecker 
  - check(operation)
- check(operation)
## zhiwei-scheduler/scheduler.py (1170L)
- is_quiet_hours(now)
- get_retry_delay(attempt)
- log_task_metrics(task_name, start_time, end_time, success, push_status, token_usage, error_msg)
- send_failure_alert(task_name, error_msg)
- setup_logging(log_dir, retention_days)
- load_prompt(template_name, **kwargs)
- call_agent(agent_id, message, timeout)
- run_ab_test(test_id)
- enrich_with_klib(task_name, prompt_text)
- fetch_rag_context(query, top_k)
- job_morning_brief()
- job_noon_brief()
- job_info_brief(hour)
- job_us_market_open()
- job_us_market_close()
- job_crypto(period)
- job_arxiv()
- job_system_check()
- job_system_metrics_report()
- job_obsidian_sync()
- job_fail_test()
- job_log_rotate()
- job_knowledge_classify()
- save_output(job_name, content)
- load_config()
- main()
- cleanup_old_files()
- schedule_retry(job_id)
- listener(event)
- signal_handler(signum, frame)
## zhiwei-scheduler/scheduler_queue.py (283L)
- atomic_write_json(path, data)
- make_job_id(task, day)
- save_result(task, content, targets, metadata, day)
- claim_file(source, dest_dir)
- try_push(file_path, push_manager, logger, return_status)
- retry_failed(push_manager, max_retries, logger)
- get_queue_stats()
- cleanup_old_files(days)
- check_and_alert(push_manager, alert_threshold, logger)
- is_already_sent(job_id)
- save_result_safe(task, content, targets, metadata, day, force)
## zhiwei-scheduler/scripts/code_skeleton.py (312L)
- get_simple_signature(node)
- get_docstring_first_line(node)
- is_skip_dunder_method(name)
- should_include_method(name)
- should_include_function(name)
- parse_file(filepath)
- scan_directories(dirs)
- generate_skeleton(files_info)
- main()
## zhiwei-scheduler/task_builder.py (250L)
- needs_clarification(intent)
- build_task(intent)
## zhiwei-scheduler/task_executor.py (542L)
- max_risk(a, b)
- assess_risk(intent)
- push_to_dingtalk(message)
- notify_review(task_id, task_data, risk_level)
- setup_dirs()
- scan_pending()
- call_claude(task_text)
- run_pre_check()
- run_backup()
- execute_task(task)
- build_result_message(task, result)
- finalize_task(task, result)
- main_loop()
- run_with_orchestrator()
- executor_wrapper(task)
## zhiwei-scheduler/task_executor_v1.py (509L)
- max_risk(a, b)
- assess_risk(intent)
- push_to_dingtalk(message)
- notify_review(task_id, task_data, risk_level)
- setup_dirs()
- scan_pending()
- call_claude(task_text)
- run_pre_check()
- run_backup()
- execute_task(task)
- build_result_message(task, result)
- finalize_task(task, result)
- main_loop()
## zhiwei-scheduler/task_orchestrator.py (229L)
- class PrioritizedTask 
  - from_json(cls, task_data)
- class TaskOrchestrator 
  - scan_pending()
  - enqueue(task)
  - enqueue_batch(tasks)
  - process_queue()
  - run_once()
  - run_loop()
  - stop()
  - status()
- dummy_executor(task)
- from_json(cls, task_data)
- scan_pending()
- enqueue(task)
- enqueue_batch(tasks)
- process_queue()
- run_once()
- run_loop()
- stop()
- status()
## zhiwei-scheduler/tasks/arxiv_tracker.py (116L)
- fetch_arxiv(categories, max_results)
- format_arxiv_markdown(papers, min_score, limit)
## zhiwei-scheduler/tasks/market.py (175L)
- fetch_us_market(symbols)
- format_market_markdown(data, report_type)
- fetch_crypto(symbols)
- format_crypto_markdown(data, alert_threshold)
## zhiwei-scheduler/tasks/system_check.py (210L)
- check_disk_usage()
- check_memory_usage()
- check_docker_containers()
- check_services()
- run_system_check(thresholds)
- format_system_markdown(report)
## zhiwei-scheduler/tasks/weather.py (101L)
- fetch_weather(city, timeout)
- format_weather_markdown(data, brief)
## zhiwei-scheduler/test.py (113L)
- run(cmd, timeout)
- test_weather()
- test_news()
- test_crypto()
- test_market()
- test_arxiv()
- test_system()
- test_push()
- test_all()
## zhiwei-scheduler/trigger_listener.py (210L)
- init(scheduler, logger, trigger_dir)
- call_agent(agent_id, message, timeout)
- execute_trigger(trigger_file)
- watch_loop(check_interval)
- start(check_interval)
- stop()
## zhiwei-scheduler/trigger_watcher.py (148L)
- call_agent(agent_id, message, timeout)
- execute_trigger(trigger_file)
- watch_triggers(interval)
- start_watcher_daemon()
